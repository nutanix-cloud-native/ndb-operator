apiVersion: v1
kind: Service
metadata:
  name: db-mssql-si-svc
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3000
      nodePort: 30001
  selector:
    app: app-mssql-si
