apiVersion: ndb.nutanix.com/v1alpha1
kind: NDBServer
metadata:
  name: ndb-mysql
spec:
  credentialSecretRef:
    name: ndb-secret-mysql-si
    namespace: ndb-credentials
  server: <https://<ip-for-ndb>:8443/era/v0.9>
  skipCertificateVerification: true
