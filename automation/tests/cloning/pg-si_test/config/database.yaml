apiVersion: ndb.nutanix.com/v1alpha1
kind: Database
metadata:
  name: clone-pg-si
spec:
  ndbRef: clone-ndb-pg-si
  isClone: true
  clone:
    name: clone-pg-si
    type: postgres
    description: Cloning pg single instance testing 
    # clusterId: ""  # Alternative: use UUID instead of name
    clusterName: ""  # Set by test framework from NX_CLUSTER_NAME_ENV
    profiles: {}
    credentialSecret: clone-db-secret-pg-si
    timezone: UTC
    # sourceDatabaseId: ""  # Alternative: use UUID instead of name
    sourceDatabaseName: ""  # Set by test framework from POSTGRES_SI_CLONING_NAME_ENV
    # snapshotId: ""  # Alternative: use specific snapshot UUID
    snapshotName: ""  # Optional: use specific snapshot name, or leave empty for auto-selection
    additionalArguments: {}
